# Example TrainingRequest, equivalent to the values in
# org.uma.evolver.example.training.TreeNSGAIIOptimizingNSGAIIForBenchmarkRE3D
#
# Exercises the tree encoding: no meta-level YAML parameter space, just the scalar
# hyperparameters of SubtreeCrossover/TreeMutation (see MetaNSGAIITreeConfiguration.yaml).

baseLevel: Re3dNSGAIITreeBaseLevel.yaml
metaSearch: MetaNSGAIITreeConfiguration.yaml
outputDirectory: results/tree-nsgaii/RE3D
writeFrequency: 100
statusFrequency: 100
# frontPlotFrequency: 100   # optional; uncomment for a live Pareto front plot (not headless)
