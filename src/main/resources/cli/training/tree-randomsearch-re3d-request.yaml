# Example TrainingRequest exercising RandomSearch as meta-optimizer with the tree encoding, on the
# same base level as tree-nsgaii-re3d-request.yaml.

baseLevel: Re3dNSGAIITreeBaseLevel.yaml
metaSearch: MetaRandomSearchTreeConfiguration.yaml
outputDirectory: results/tree-randomsearch/RE3D
writeFrequency: 100
statusFrequency: 100
# frontPlotFrequency: 100   # optional; uncomment for a live Pareto front plot (not headless)
